import ArticlePage from "@/components/blogUI/ArticlePage";
import React from "react";

const ArticlePageWrapper = ({ params }) => {
  return <ArticlePage params={params} />;
};

export default ArticlePageWrapper;
